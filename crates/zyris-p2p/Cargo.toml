[package]
name = "zyris-p2p"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Transport that carries Zyris over a direct node-to-node connection"
readme = "README.md"
keywords = ["zyris", "p2p", "quic", "iroh", "transport"]
categories = ["network-programming", "asynchronous"]

[dependencies]
async-trait.workspace = true
bytes.workspace = true
iroh = "1"
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["rt", "sync", "macros", "time", "io-util", "fs"] }
tracing.workspace = true
zyris = { version = "0.2.0", package = "zyris-core", path = "../zyris-core" }
zyris-proto = { version = "0.2.0", path = "../zyris-proto" }

[dev-dependencies]
async-trait.workspace = true
# Only so `tls`'s tests can ask whether a default provider exists. The module itself names no
# rustls type: it installs whatever `iroh` picked, so the two can never disagree.
rustls = { version = "0.23", default-features = false }
schemars.workspace = true
serde.workspace = true
tempfile.workspace = true
tokio = { workspace = true, features = ["full"] }

[package.metadata.docs.rs]
all-features = true
